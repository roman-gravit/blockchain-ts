{
  "name": "blockchain-ts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "serve",
    "compileDeploy": "tsc && npm run deploy",
    "deploy": "copyfiles -f src/browser/*.html src/browser/*.css dist",
    "node": "tsc -p src/node/tsconfig.json"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.5",
    "copyfiles": "^2.4.1",
    "serve": "^14.2.3",
    "typescript": "^5.6.2"
  }
}
